source "https://rubygems.org"

gem "jekyll", "~> 4.3"
# Pinned to 7.4.1: 7.5+ switched to Bootstrap 5.3 `data-bs-theme` and the
# default rendered dark on the deployed site despite `theme_mode: light`.
# Bump deliberately (and re-verify light mode) rather than floating across 7.x.
gem "jekyll-theme-chirpy", "= 7.4.1"
gem "csv"
gem "base64"
gem "bigdecimal"

group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-archives"
end
